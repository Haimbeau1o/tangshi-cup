import { PlayerLibraryManager } from "@/components/content/player-library-manager";

export const metadata = {
  title: "选手库",
};

export default function PlayersPage() {
  return <PlayerLibraryManager />;
}
